{% extends 'base.html.twig' %}

{% block body %}
    <h1>Admin Dashboard</h1>
    <div class="admin-dashboard">
        <table class="admin-table" id="admin-table">
            <thead>
                <tr>
                    <th>Rezept</th>
                    <th>Ersteller</th>
                    <th>Beschreibung</th>
                    <th>Bild</th>
                    <th>Optionen</th>
                </tr>
            </thead>
            <tbody>
                {% for recipe in recipes %}
                    <tr>
                        <td>{{ recipe.name }}</td>
                        <td>{{ recipe.user.username | default("")}}</td>
                        <td>{{ recipe.description }}</td>
                        <td>
                        {% if recipe.image %}
                            <div class="recipe-image">
                                <img src="{{ recipe.image }}" alt="Bild des Rezepts" class="recipe-image" id="recipe-image">
                            </div>
                        {% endif %}
                        <td>
                            <div class="button-area">
                                <button class="btn secondary ghost" id="showRecipe" data-id="{{ recipe.id }}">Anzeigen</button>
                                <button class="btn secondary ghost" id="editRecipe" data-id="{{ recipe.id }}">Bearbeiten</button>
                                <button class="btn secondary ghost" id="deleteRecipe" data-id="{{ recipe.id }}">Löschen</button>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <h2>Pending Users</h2>
        <table class="admin-table" id="pending-users-table">
            <thead>
                <tr>
                    <th>Username</th>
                    <th>Optionen</th>
                </tr>
            </thead>
            <tbody>
                {% for user in pendingUsers %}
                    <tr>
                        <td>{{ user.username }}</td>
                        <td>
                            <div class="button-area">
                                <button class="btn secondary ghost" id="approveUser" data-id="{{ user.id }}">Approve</button>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {{ encore_entry_script_tags('adminDashboard') }}
{% endblock %}
